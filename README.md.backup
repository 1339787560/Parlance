# LAN InfoShare

局域网内的轻量信息共享服务，支持实时聊天、文件上传、ZIP 打包、多主题切换和消息撤回。

## 功能

- 实时聊天：基于 SSE 推送消息更新
- 文件传输：支持单文件上传、下载和断点续传
- 多文件上传：自动打包为 ZIP
- 自定义昵称：按 IP 保存显示名
- 主题切换：按 IP 保存主题偏好
- 消息管理：撤回自己的消息、清空全部消息
- 局域网访问：适合内网设备共享

## 快速开始

安装依赖：

```bash
pip install -r requirements.txt
```

启动服务：

```bash
python main.py
```

默认监听地址由 `config.yaml` 中的 `server.host` 和 `server.port` 决定。

## 配置

编辑 `config.yaml`：

```yaml
server:
  host: 0.0.0.0
  port: 5000
  upload_dir: ./uploads

database:
  path: ./data/chat.db

friendship_services: []
```

## API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/` | 首页 |
| GET | `/api/messages` | 获取消息列表 |
| POST | `/api/messages/text` | 发送文本消息 |
| POST | `/api/messages/file` | 上传文件 |
| POST | `/api/messages/zip` | 上传多文件并打包 |
| DELETE | `/api/messages/{id}` | 撤回自己的消息 |
| DELETE | `/api/messages?confirm=true` | 清空全部消息 |
| GET | `/api/download/{id}` | 下载文件 |
| GET | `/api/events` | SSE 实时事件 |
| GET | `/api/theme` | 获取主题偏好 |
| POST | `/api/theme` | 保存主题偏好 |
| GET | `/api/profile` | 获取昵称 |
| POST | `/api/profile` | 保存昵称 |
| GET | `/api/users` | 获取活跃用户 |
| GET | `/api/whoami` | 获取当前 IP |
| GET | `/api/health` | 健康检查 |

## 技术栈

- FastAPI
- Uvicorn
- SQLite
- SSE

